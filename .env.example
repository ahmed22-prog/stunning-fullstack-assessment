# Primary provider — OpenAI. Required.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra

# Fallback provider — Gemini. Optional: without it, an OpenAI outage surfaces
# as an error instead of falling back.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
